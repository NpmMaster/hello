const { App } = require('./util.js')



class Table {
  process.argv.ForEach(constructor(*items, color) {
    this._items=items
    this._color=color
    try {
      divi = this._items.split()
      dividez = divi.join('  ')
      app = new App(dividez, option=this._color)
    } catch((error) => {console.log('Error while running table mode')}
  }
}
